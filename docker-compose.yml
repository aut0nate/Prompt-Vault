services:
  prompt-vault:
    container_name: prompt-vault
    build:
      context: .
      args:
        DATABASE_URL: file:/app/data/dev.db
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_URL: file:/app/data/dev.db
      ATTACHMENTS_DIR: /app/data/prompt-attachments
    volumes:
      - prompt-vault-data:/app/data
    networks:
      - edge-net
    logging:
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      com.centurylinklabs.watchtower.enable: "true"

volumes:
  prompt-vault-data:

networks:
  edge-net:
    external: true
